<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements, required for notarization.

  Each one is here because something in the app needs it, not by default:
    - allow-jit / allow-unsigned-executable-memory: V8 generates machine code at runtime.
      Without these, Electron's JavaScript engine cannot run under the hardened runtime.
    - disable-library-validation: DuckDB and llama.cpp load native libraries. They are
      signed as part of the bundle, but llama.cpp's Metal backend loads helpers that
      library validation would otherwise refuse.

  Deliberately absent: network server, camera, microphone, contacts, location. Datera
  asks for nothing it does not use, and every entitlement here is visible to anyone who
  inspects the signed app.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
